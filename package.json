{
  "name": "bloom",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "evolve": "tsx src/index.ts",
    "assess": "tsx src/assess.ts",
    "stats": "tsx src/stats.ts",
    "journal": "tsx src/journal.ts",
    "roadmap": "tsx src/roadmap.ts",
    "generate-pages": "tsx scripts/generate-pages.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "latest",
    "better-sqlite3": "^12.6.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20",
    "tsx": "^4.0",
    "typescript": "^5.4",
    "vitest": "^2.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}